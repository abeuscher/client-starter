{
  "name": "starter-template",
  "version": "1.0.0",
  "description": "Starter Site Theme",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.js",
    "silent": "node build.js --silent",
    "watch": "node build.js --watch",
    "production": "node build.js --production",
    "setup": "./setup.sh",
    "tree": "find . -maxdepth 8 -type d \\( -name \"node_modules\" -o -name \".git\" \\) -prune -o -print"
  },
  "author": "Alex Beuscher",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.4",
    "esbuild-plugin-pug": "^0.0.8",
    "fs": "^0.0.2",
    "fs-extra": "^11.1.1",
    "path": "^0.12.7",
    "prettier": "^3.0.3",
    "pug": "^3.0.2",
    "pug-php-filter": "^0.0.1",
    "sass": "^1.67.0"
  },
  "dependencies": {
    "chokidar": "^4.0.3",
    "dotenv": "^16.5.0"
  }
}
